# No required environment variables for the current MVP.
#
# The app currently reads delayed market data from Stooq and falls back to
# local mock data if the request fails. Add provider keys here later, for example:
#
# FINNHUB_API_KEY=
# ALPHA_VANTAGE_API_KEY=
